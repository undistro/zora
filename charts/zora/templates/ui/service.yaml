apiVersion: v1
kind: Service
metadata:
  name: {{ include "zora.fullname" . }}-ui
  labels:
    {{- include "zora.uiLabels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "zora.uiSelectorLabels" . | nindent 4 }}
