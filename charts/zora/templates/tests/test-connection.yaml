apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "zora.fullname" . }}-test-connection"
  labels:
    {{- include "zora.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "zora.fullname" . }}-server:{{ .Values.server.service.port }}']
  restartPolicy: Never
