apiVersion: v1
kind: Service
metadata:
  name: {{ include "zora.fullname" . }}-metrics
  labels:
    {{- include "zora.operatorLabels" . | nindent 4 }}
spec:
  type: {{ .Values.operator.metricsService.type }}
  ports:
    - name: https
      port: {{ .Values.operator.metricsService.port }}
      protocol: TCP
      targetPort: https
  selector:
    {{- include "zora.operatorSelectorLabels" . | nindent 4 }}
