apiVersion: v1
kind: Service
metadata:
  name: {{ include "inspect.fullname" . }}-server
  labels:
    {{- include "inspect.serverLabels" . | nindent 4 }}
spec:
  type: {{ .Values.server.service.type }}
  ports:
    - port: {{ .Values.server.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inspect.serverSelectorLabels" . | nindent 4 }}
