{{- if .Values.server.rbac.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "inspect.serverServiceAccountName" . }}
  labels:
    {{- include "inspect.serverLabels" . | nindent 4 }}
  {{- with .Values.server.rbac.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{ end }}
{{- if .Values.server.rbac.create -}}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "inspect.serverServiceAccountName" . }}
rules:
  - apiGroups:
      - inspect.undistro.io
    resources:
      - clusters
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - inspect.undistro.io
    resources:
      - clusters/status
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "inspect.serverServiceAccountName" . }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "inspect.serverServiceAccountName" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "inspect.serverServiceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
