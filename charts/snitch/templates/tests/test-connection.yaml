apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inspect.fullname" . }}-test-connection"
  labels:
    {{- include "inspect.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inspect.fullname" . }}-server:{{ .Values.server.service.port }}']
  restartPolicy: Never
