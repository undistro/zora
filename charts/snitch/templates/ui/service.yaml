apiVersion: v1
kind: Service
metadata:
  name: {{ include "inspect.fullname" . }}-ui
  labels:
    {{- include "inspect.uiLabels" . | nindent 4 }}
spec:
  type: {{ .Values.ui.service.type }}
  ports:
    - port: {{ .Values.ui.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inspect.uiSelectorLabels" . | nindent 4 }}
